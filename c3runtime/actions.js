"use strict";
const C3 = globalThis.C3
{
    // var received = false;
    globalThis.C3.Plugins.iDoveHelper_BiliOpen.Acts = {
        QuickConnect(AuthCode, LiveRoomID) {
           this.QuickConnect(AuthCode, LiveRoomID)
        },
        AdvancedConnect(AuthCode, AppID, DeveloperKey, DeveloperSecret, LiveRoomID) {
           this.AdvancedConnect(AuthCode, AppID, DeveloperKey, DeveloperSecret, LiveRoomID)
        }
    }
}
